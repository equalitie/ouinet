diff -u -r boost_1_71_0.orig/tools/build/src/tools/darwin.jam boost_1_71_0/tools/build/src/tools/darwin.jam
--- boost_1_71_0.orig/tools/build/src/tools/darwin.jam	2023-07-04 17:59:00
+++ boost_1_71_0/tools/build/src/tools/darwin.jam	2023-07-04 18:01:31
@@ -137,13 +137,14 @@
     # - Set the toolset generic common options.
     common.handle-options darwin : $(condition) : $(command) : $(options) ;
     
+    real-version = [ regex.split $(real-version) \\. ] ;
     # - GCC 4.0 and higher in Darwin does not have -fcoalesce-templates.
-    if $(real-version) < "4.0.0"
+    if [ version.version-less $(real-version) : 4 0 ]
     {
         flags darwin.compile.c++ OPTIONS $(condition) : -fcoalesce-templates ;
     }
     # - GCC 4.2 and higher in Darwin does not have -Wno-long-double.
-    if $(real-version) < "4.2.0"
+    if [ version.version-less $(real-version) : 4 2 ]
     {
         flags darwin.compile OPTIONS $(condition) : -Wno-long-double ;
     }